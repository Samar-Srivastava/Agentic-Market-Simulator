{
  "compilerOptions": {
    /* ... your existing settings here ... */
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    },

    // 👇 add these lines to relax the build temporarily
    "skipLibCheck": true,
    "noUnusedLocals": false
  },

  // leave these as-is
  "files": [],
  "include": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ]
}
